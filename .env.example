# Supabase – create project at https://supabase.com and copy from Settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: for admin-only features you can add RLS and use service role in server/edge
# For client-only admin, use Supabase Auth (email/password or magic link)
